import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    menu_option: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    form_label: {
        color: 'black',
        fontSize: 12,
    },
});
